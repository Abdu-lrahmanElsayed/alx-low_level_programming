#include "main.h"
#include<stdio.h>
/**
 *factorial - prints factorial
 *@n: a num
 *Return: fac
 */
int factorial(int n)
{
	int i ,j;
	/*variabls for fac*/
	i = 1;
	j = 1;
	if (n == 0)
	{
		return (1);
	}
	if (n < 0)
	{
		return (-1);
	}
}
