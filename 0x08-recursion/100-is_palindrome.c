#include "main.h"
#include<stdio.h>
#include<string.h>
/**
 *is_palindrome - check string
 *@s: input string
 *Return: 0 or 1
 */
int is_palindrome(char *s)
{
	if (*s == '\0')
	{
		return (1);
	}
	else
		return (0);
}
