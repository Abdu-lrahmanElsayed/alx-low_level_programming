#include "main.h"

/**
 * more_numbers - prints 10 times the numbers, from 0 to 14, followed by a new line.
 *
 * Return: nums.
 */
void more_numbers(void)
{
	int nums;
	
	while (int i; i <= 10; i++)
	{
		for (nums <= 14)
		{
			_putchar(nums '0');
		}
		_putchar('\n');
	}
	_putchar('\n');
}
