#include<stdio.h>
#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @int: input num
 * @int: input num
 * Return: The result of sum
 */
int add(int, int)
{
	int a,b,sum;

	sum = a + b;
	return (sum);
}
