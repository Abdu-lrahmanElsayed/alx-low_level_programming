#include<stdio.h>
#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @a: input num
 * @b: input num
 * Return: The result of sum
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
